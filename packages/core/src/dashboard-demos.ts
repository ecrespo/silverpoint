/**
 * `@silverpoint/core/dashboard-demos`: the reference dashboards of Data Model §4, for fixtures,
 * example apps and documentation. Apart from the main entry, so no application ships them (REQ-164).
 */
export { DASHBOARD_DEMOS, type DashboardDemo, type DashboardDemoChild } from './dashboard/demo';
